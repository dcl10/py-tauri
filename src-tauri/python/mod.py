def add(a: int | float, b: int | float) -> int | float:
    """Add to numbers, `a` and `b`, togther.

    Args:
        a (int | float): `a`
        b (int | float): `b`

    Returns:
        int | float: `a + b`
    """
    return a + b
